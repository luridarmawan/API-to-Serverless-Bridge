const test = require('./test');
const echoController = require('./echo_controller');
const promiseController = require('./promise_controller');

module.exports = {
    test,
    echoController,
    promiseController
}
